// deta databases per module / feature
// analogous to Table name or Collection name
// ! should not be changed when set and used
class DetaBases {
  static const String kFacultyDptCollection = 'faculty_dptDB';
}
